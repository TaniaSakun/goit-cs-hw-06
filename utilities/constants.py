class Constants:
    # Messages
    failed_to_send_data = "Failed to send data"
    server_stopped_error = "Server stopped due to a KeyboardInterrupt"
    server_started = "Server started: http://{0} : {1}"
    server_stopped = "Server stopped"
    http_server = "http_server"
    socket_server_stopped_error = "Socket server stopped due to a KeyboardInterrupt"
    socket_server_started = "Server started: socket://{0}:{1}"
    socket_server_stopped = "Socket server stopped"
    socket_server = "socket_server"
    received_data = "Received data from {0}: {1}"

    # Headers
    content_length = "Content-Length"
    content_type = "Content-Type"
    html_content_type = "text/html"
    plain_content_type = "text/plain"

    # Formats
    datetime_format = "%Y-%m-%d %H:%M:%S.%f"
